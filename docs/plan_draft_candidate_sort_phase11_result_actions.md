# 2026ドラフト候補ソート page phase 11 結果画面の操作

## 実行内容

phase 11 として、`/draft-candidate-sort/result` の結果画面操作を実装した。

## 更新ファイル

- `app/draft-candidate-sort/result/DraftCandidateSortResult.tsx`

## 実装した操作

結果画面では以下を操作できる。

- 番付表を見る
- ドラフト順位予想表を見る
- 未評価が多い候補を見る
- PNG保存
- テキストコピー
- もう一度ソートする

## タブ

結果表示は以下のタブに分ける。

- 番付表
- ドラフト順位予想表
- 未評価が多い候補

初期表示は番付表とする。

## PNG保存

初期実装では、結果テキストをキャンバスに描画し、PNGとして保存する。

保存ファイル名は以下。

```text
draft-candidate-sort-result.png
```

## テキストコピー

番付表、ドラフト順位予想表、未評価が多い候補をまとめたテキストをクリップボードへコピーする。

コピー完了時は `コピーしました` と表示する。

## もう一度ソートする

localStorage の保存データを削除し、`/draft-candidate-sort` へ戻る。

## 検証結果

2026-09-17 に以下を確認した。

- `/draft-candidate-sort/result`: 200
- タブ状態を保持できる
- PNG保存関数が存在する
- テキスト生成関数が存在する
- もう一度ソートする処理で保存キーを削除する

## phase 11 完了条件

- 番付表タブを表示できる
- ドラフト順位予想表タブを表示できる
- 未評価が多い候補タブを表示できる
- PNG保存ができる
- テキストコピーができる
- もう一度ソートできる

phase 11 は完了とする。
